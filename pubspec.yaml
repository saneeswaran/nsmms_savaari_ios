name: namma_savaari
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+39

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  animate_do: ^4.2.0
  carousel_slider: ^5.0.0
  cloud_firestore: ^6.0.2
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.1.0
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  firebase_storage: ^13.0.2
  flutter_local_notifications: ^19.4.2
  flutter_rating_bar: ^4.0.1
  flutter_typeahead: ^5.2.0
  fluttertoast: ^8.2.8
  go_router: ^16.2.4
  google_sign_in: ^7.1.1
  http: ^1.2.2
  intl: ^0.20.2
  provider: ^6.1.5
  razorpay_flutter: ^1.4.0
  shared_preferences: ^2.5.3
  shimmer: ^3.0.0
  url_launcher: ^6.3.1
  video_player: ^2.9.2
  youtube_player_flutter: ^9.1.1
  flutter_native_splash: ^2.4.6
  video_thumbnail: ^0.5.6
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/
    - assets/bus_gif_2.gif
    - assets/gif.gif
    - assets/bus_im.jpg
    - assets/google_logo_officiall.png
    - assets/red_bus_caro_1.jpeg
    - assets/redbus_caro_2.jpeg
    - assets/redbus_caro_3.jpeg
    - assets/red_bus_caro_4.jpeg
    - assets/phone_number.jpg
    - assets/Flag_of_India.svg.png
    - assets/us_flag.png
    - assets/Flag_of_the_United_Kingdom_(1-2).svg.png
    - assets/Flag_of_Australia_(converted).svg.png
    - assets/reate_review_pic.jpg
    - assets/rate_rev.JPG
    - assets/profile.icon.jpg
    - assets/redbuslogo.JPG
    - assets/bus_image_technical_support.png
    - assets/yello_bus_gif.gif
    - assets/day_bus.gif
    - assets/moving_bus.gif
    - assets/pink_bus_icon.png
    - assets/customer_profile.jpg
    - assets/Add a heading.jpg
    - assets/sell_clothings_main_image.jpeg
    - assets/sell_electronics_main_image.jpg
    - assets/sell_furniture_main_image.jpeg
    - assets/sell_footwear_main_image_1.jpeg
    - assets/steering-wheel.png
    - assets/Namma_Savaari_LOGO1.png
    - assets/Namma_Savaari.png
    - assets/playstore.png
    - assets/appstore.png
    - assets/facebook.png
    - assets/linkedin.png
    - assets/twitter.png
    - assets/instagram.png
    - assets/NammaSavaariQR.png
    - assets/cover_picture1.jpg
    - assets/user.png
    - assets/video_thumbnail.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: DK More Or Less
      fonts:
        - asset: assets/fonts/DKMoreOrLess.otf
    #         - asset: assets/fonts/Exo2-Italic-VariableFont_wght.ttf
    #           style: italic
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat-Regular.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
